PORT=4000

# Coston2-funded key shared by every on-chain-touching backend service
# (compliance attester, relayer, dark-engine matcher) — see chain.ts for why
# that's a testnet-only shortcut, not a production trust model.
PRIVATE_KEY=
