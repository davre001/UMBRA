{
  "name": "umbra-matcher-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone match_orders proving worker — deliberately NOT part of the backend/ deployment (see its own README). Run this wherever you have real compute for Noir/Barretenberg proving; it talks to a running backend over HTTP.",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"matcher-worker build ok\"",
    "sync-circuit": "node scripts/sync-circuit.js",
    "package-lambda": "pnpm run build && pnpm run sync-circuit && node scripts/package-lambda.js",
    "deploy-lambda": "bash scripts/deploy-lambda.sh"
  },
  "dependencies": {
    "@aztec/bb.js": "5.0.0-nightly.20260522",
    "@noir-lang/noir_js": "1.0.0-beta.22",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
