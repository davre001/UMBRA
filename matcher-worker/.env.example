# Base URL of the running backend (dark-engine routes) to poll.
BACKEND_URL=http://localhost:4000

# Must match the backend's MATCHER_INTERNAL_SECRET — see backend/.env.example.
MATCHER_INTERNAL_SECRET=

# Milliseconds between poll cycles.
POLL_INTERVAL_MS=5000
